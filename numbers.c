#include "main.h"
/**
 * add_numbers - Add two numbers
 * @num1: First number
 * @num2: Second number
 * Return: The sum of num1 and num2
 */
int add_numbers(int num1, int num2)
{
	int sum;

	sum = num1 + num2;
	return (sum);
}
